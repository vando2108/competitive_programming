#include <bits/stdc++.h>

using namespace std;

void kdimo() {
  int n;  cin >> n;
  vector<int> a(n, 0);
  for (auto &it: a) cin >> it;
}

int main() {
  ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

  int testCase;
  cin >> testCase;



  return 0;
}
