#include <bits/stdc++.h>

using namespace std;

void kdimo() {
	int n;  cin >> n;
	string s;  cin >> s;
	for (int i = 0; i < n; i++) {
	}
}

int main() {
	ios_base::sync_with_stdio(false);  cin.tie(NULL);  cout.tie(NULL);

	int testCase;  
	cin >> testCase;
	while (testCase--) {
		kdimo();
	}

	return 0;
}

